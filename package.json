{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "release": "lerna publish",
    "test": "yarn lint && jest"
  },
  "devDependencies": {
    "eslint": "7.27.0",
    "jest": "27.0.1",
    "lerna": "4.0.0"
  },
  "dependencies": {
    "eslint-plugin-jest": "^24.3.6"
  }
}
