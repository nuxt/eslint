{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "release": "lerna publish",
    "test": "yarn lint && jest"
  },
  "devDependencies": {
    "eslint": "7.28.0",
    "jest": "27.0.4",
    "lerna": "4.0.0"
  },
  "dependencies": {
    "eslint-plugin-jest": "^24.3.6"
  }
}
