{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "test": "yarn lint && jest",
    "release": "lerna publish"
  },
  "devDependencies": {
    "eslint": "latest",
    "jest": "latest",
    "lerna": "latest"
  }
}
