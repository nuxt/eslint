{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "release": "lerna publish",
    "test": "yarn lint && jest"
  },
  "devDependencies": {
    "eslint": "7.21.0",
    "jest": "26.6.3",
    "lerna": "4.0.0"
  },
  "dependencies": {
    "eslint-plugin-jest": "^24.1.5"
  }
}
