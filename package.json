{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "eslint . --config=packages/eslint-config/index.js",
    "release": "lerna publish",
    "test": "yarn lint && jest"
  },
  "devDependencies": {
    "eslint": "6.7.1",
    "jest": "25.1.0",
    "lerna": "3.19.0"
  }
}
